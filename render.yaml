services:
  - type: web
    name: cosmos-exoplanet-app
    env: python
    buildCommand: "pip install -r requirements.txt && python -m src.ml.train"
    startCommand: "gunicorn --workers 2 --threads 2 --bind 0.0.0.0:$PORT app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.12
